name: Pipeline [test -> build -> deploy]

on:
  push:
    branches-ignore:
      - 'dependabot/**'
      - 'main'

jobs:
  python_lint:
    name: python lint
    uses: ministryofjustice/hmpps-github-actions/.github/workflows/test_python_lint.yml@FEAT/HEAT-864-add-python-ruff-formatter-workflow # WORKFLOW VERSION
    secrets: inherit
